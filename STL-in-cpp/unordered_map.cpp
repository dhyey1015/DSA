#include <bits/stdc++.h>
using namespace std;

int main(){
    unordered_map<int, int>mpp;
    // it is similar to map only things are:
    // the map will not be sorted and map will have unique keys 
    return 0;
}