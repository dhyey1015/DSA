#include <bits/stdc++.h>
using namespace std;

int main(){
    multimap<int, int> mpp;

    //everything is same as map only it can store multiple keys 
    //we can store dublicate keys over here 
    // we cand do the {{1,2}, {1,3}}
    return 0;
}